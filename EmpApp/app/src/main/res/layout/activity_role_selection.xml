<!-- res/layout/activity_role_selection.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="@drawable/select">

    <!-- TextView for Project Name -->
    <TextView
        android:id="@+id/text_project_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Employee Task Management System"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="20dp"
        android:textColor="#000000" /> <!-- You can adjust the color as needed -->

    <!-- TextView for Role Selection -->
    <TextView
        android:id="@+id/text_role_selection"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Your Role"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="30dp"
        android:textColor="#000000"/> <!-- Adjust color if needed -->

    <!-- Button for Employee -->
    <Button
        android:id="@+id/button_employee"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Employee"
        android:textSize="20sp"
        android:layout_marginBottom="10dp"/>

    <!-- Button for Admin -->
    <Button
        android:id="@+id/button_admin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Admin"
        android:textSize="20sp"/>
</LinearLayout>
